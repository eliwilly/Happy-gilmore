<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>Happy Gilmore's Golf Showdown</title>
<style>
  body { font-family: system-ui, sans-serif; max-width: 480px; margin: 20px auto; padding: 0 10px; }
  h1 { text-align: center; }
  button { font-size: 1.2em; margin: 8px; padding: 10px 20px; }
  #output { margin-top: 20px; white-space: pre-wrap; font-size: 1.1em; line-height: 1.3; }
</style>
</head>
<body>
<h1>🏌️ Happy Gilmore's Golf Showdown 🏌️‍♂️</h1>
<div id="status"></div>
<div>
  <button id="happySwing">Happy Swing (Powerful but unpredictable)</button>
  <button id="normalSwing">Normal Swing (More accurate)</button>
</div>
<div id="output"></div>

<script>
(function() {
  const statusEl = document.getElementById('status');
  const outputEl = document.getElementById('output');
  const happyBtn = document.getElementById('happySwing');
  const normalBtn = document.getElementById('normalSwing');

  function randRange(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
  }

  let holeDistance = randRange(300, 450);
  let strokes = 0;
  let position = 0;
  let gameOver = false;

  function log(msg) {
    outputEl.textContent += msg + "\\n";
    outputEl.scrollTop = outputEl.scrollHeight;
  }

  function resetGame() {
    holeDistance = randRange(300, 450);
    strokes = 0;
    position = 0;
    gameOver = false;
    outputEl.textContent = "";
    statusEl.textContent = `The hole is ${holeDistance} yards away. Try to reach it in as few shots as possible!\\n`;
  }

  function happySwing() {
    const base = randRange(100, 300);
    const chaos = randRange(-50, 50);
    return base + chaos;
  }

  function normalSwing() {
    const base = randRange(80, 150);
    const accuracy = randRange(-10, 10);
    return base + accuracy;
  }

  function takeShot(swingType) {
    if (gameOver) return;
    let distance = 0;
